import { NestMiddleware } from "@nestjs/common";
import * as helmet from "helmet";
type ContentSecurityPolicyOptions = Parameters<typeof helmet.contentSecurityPolicy>[0];
export declare class HelmetContentSecurityPolicyMiddleware implements NestMiddleware {
    static configure(opts: ContentSecurityPolicyOptions): void;
    private static options;
    use(req: any, res: any, next: any): void;
}
export {};
