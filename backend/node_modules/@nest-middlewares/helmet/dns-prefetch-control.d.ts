import { NestMiddleware } from "@nestjs/common";
import * as helmet from "helmet";
type XDnsPrefetchControlOptions = Parameters<typeof helmet.dnsPrefetchControl>[0];
export declare class HelmetDnsPrefetchControlMiddleware implements NestMiddleware {
    static configure(opts: XDnsPrefetchControlOptions): void;
    private static options;
    use(req: any, res: any, next: any): void;
}
export {};
